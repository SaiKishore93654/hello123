name: "Multi project"

on: push

jobs:
  build_jobs:
     runs-on: ubuntu/latest
     steps:
     - name: Hello world
       run: echo "Hello world"
     - name: test world
       run: echo "test world"  